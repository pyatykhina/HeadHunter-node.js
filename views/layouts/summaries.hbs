<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{{htmlWebpackPlugin.options.title}}</title>
        <link rel='stylesheet' href='/assets/css/style.css'>
    </head>
    <body>
        <div class="container">
            {{> header}}
            <div class="content summaries">
                {{> search}}
                {{> categories}}
                <div class="summaries__main">
                    {{> filters}}
                    <ul class="summaries__list">
                        
                    </ul>
                </div>
                <form class="form" action="/summaries/add" method="POST" enctype="application/x-www-form-urlencoded">
                    {{> form}}
                </form>
            </div>
            {{> footer}}
        </div>
    </body>
</html>